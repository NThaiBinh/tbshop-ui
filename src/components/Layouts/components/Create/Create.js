import Modal from '../Modal/Modal'
import cssCreate from './Create.module.css'

function Create(props) {
   return (
      <Modal>
         <div className={cssCreate.wrapper}>CREATE</div>
      </Modal>
   )
}

export default Create
